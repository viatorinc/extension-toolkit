<%= name %>test.ts
import VueCompositionAPI from '@vue/composition-api';
import { shallowMount, createLocalVue } from '@vue/test-utils';
import Interface<%= nameCamelFull %> from './<%= nameSnake %>.vue';

import VInput from '@/components/v-input';

const localVue = createLocalVue();
localVue.use(VueCompositionAPI);
localVue.component('v-input', VInput);

describe('Interfaces / <%= name %>', () => {
	it('Renders a v-input', () => {
		const component = shallowMount(Interface<%= nameCamelFull %>, {
			localVue,
			propsData: {
				options: {}
			},
			listeners: {
				input: () => {}
			}
		});
		expect(component.find(VInput).exists()).toBe(true);
	});
});