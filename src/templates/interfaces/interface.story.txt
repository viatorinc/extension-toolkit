<%= name %>story.ts
import { withKnobs, text, number, boolean } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import Vue from 'vue';
import Interface<%= nameCamelFull %> from './<%= nameSnake %>.vue';
import markdown from './readme.md';
import withPadding from '../../../.storybook/decorators/with-padding';
import { defineComponent } from '@vue/composition-api';

Vue.component('interface-<%= nameSnake %>', Interface<%= nameCamelFull %>);

export default {
	title: 'Interfaces / <%= name %>',
	decorators: [withKnobs, withPadding],
	parameters: {
		notes: markdown
	}
};

export const basic = () =>
	defineComponent({
		props: {
			value: {
				default: text('Value', 'Default Value')
			}
		},
		setup() {
			const onInput = action('input');
			return { onInput };
		},
		template: `
            <interface-<%= nameSnake %>
				:value="value"
				@input="onInput"
			/>
		`
	});